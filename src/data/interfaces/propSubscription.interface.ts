/**
 * Interface for the Property Subscription schema structure
 */

export interface PropSubscriptionSchema {
    destinationContextObjectName?: string;
    destinationTargetName?: string;
    isIgnored?: string;
}