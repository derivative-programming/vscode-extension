/**
 * Interface for the User Story schema structure
 */

export interface UserStorySchema {
    name: string;
    storyNumber: string;
    storyText: string;
    isIgnored: string;
    isStoryProcessed: string;
}