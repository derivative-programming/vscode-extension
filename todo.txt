Agent todo items...


 
- in table view show columns in alphabetical order, but 'name' should be first


- In the object details tabs, if the schema has a description for 
a property\setting, show the description when the user hovers over it.


- properties tab on list view and table view the 'add property' button
should be on the same line as the two 'view' buttons, but right justified.

- properties tab list view should show no controls to the right of the list view if
no item in the list control is selected. 

